apiVersion: v1
kind: Service
metadata:
  name: {{ include "reportiq.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "reportiq.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "reportiq.selectorLabels" . | nindent 4 }}
